// ============================================================
//  CARICAMENTO DATI — fetch data.json all'avvio
// ============================================================

// ============================================================
//  CARICAMENTO DATI
// ============================================================
document.addEventListener("DOMContentLoaded", () => {
  initTheme();
  loadMioListone();
  loadAcquisti();
  loadData();
  initRippleEffect();
});
